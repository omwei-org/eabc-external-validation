# PAMIR ARGUS × EABC — IV-001 Fixture Bundle v0.2

Source release preserved as received from the dedicated public IV-001 validation repository:

https://github.com/yigitcan-ozturk/pamir-argus-iv001-validation

Source release repository:
`yigitcan-ozturk/pamir-argus-iv001-validation`

Release version: `0.2`
Source branch: `main`

The private ARGUS development/baseline repository is not the source for this release and remains unchanged.

This directory preserves the serialized IV-001 corpus, six A–F fixtures, the source SHA-256 manifest, and the source README. Fixture contents are not modified by the EABC external-validation repository.

Critical seam:
`Exact Governed Action → Authorization → Commit → Exact Effect Correlation → Observed Effect → Downstream Reconstruction`

The `effect_correlation` object is an EXPERIMENTAL IV-001 validation artifact. It is not a new normative EABC field set and is not asserted to be a canonical ARGUS schema.

Source manifest: `SHA256SUMS.csv`

The SHA-256 values in the source manifest are authoritative for the source release. Any EABC-side verification result should be recorded separately from the preserved source material.
